# SATAG - Plantilla de variables de entorno.
# Copia este archivo a .env.local y rellena con los valores de Supabase (Settings -> API).
# .env.local esta en .gitignore; este .env.example si se versiona (sin secretos).

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
