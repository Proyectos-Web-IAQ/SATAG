# --- Datos personales / insumos: NO versionar ---
Insumos/
# Exports del registro real de campo (nombres + placas): jamas versionar
Campo/datos/
# Padron operativo real (nombres + placas de la comunidad): jamas versionar
Acceso a estacionamiento.xlsx
/*.heic
/*.jpg
/*.jpeg
/*.png

# --- Estándar interno de gestión de proyectos (no se versiona en este repo) ---
/*.docx

# --- Secretos / llaves API: JAMAS versionar ---
APIkeys.txt
*.key
*.pem

# --- Configuracion local de herramientas de escritorio: no se versiona ---
.claude/
.agents/
.cursor/
.vscode/

# --- Sistema operativo / temporales ---
Thumbs.db
desktop.ini
.DS_Store
~$*
*.tmp

# --- Dependencias / build (Next.js) ---
node_modules/
.next/
out/
dist/
build/
*.tsbuildinfo
.env
.env.local

# --- Tandas generadas para armar staging (salida de supabase/staging/armar-tandas.ps1) ---
supabase/staging/salida/

# --- Pruebas de carga: el CSV crudo de k6 pesa cientos de MB; se conservan summary.json y analisis.md ---
pruebas-carga/resultados/*.csv
pruebas-carga/.env.staging
pruebas-carga/bin/

# Temporales de la conversion a Word (_herramientas/md2docx.ps1)
*.tmp.html
