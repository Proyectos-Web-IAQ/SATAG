# Bitácora de Ejecución de Pruebas — SATAG

> Registro de evidencia de la actividad **WBS 1.8**. Se llena **durante** la ejecución, un renglón por
> caso de la [matriz](01%20-%20Matriz%20de%20Casos.md). Es el entregable que respalda el acta de cierre.

| | |
|---|---|
| **Versión probada** | `dc37b54` — árbol de trabajo limpio · **reejecución parcial del 11-ago sobre `cdae55e`** |
| **Entorno** | Proyecto Supabase de desarrollo/QA |
| **Ejecutó** | Gerardo Sánchez — Soporte TI Jr. |
| **Atestiguó** | *(auditor, cuando aplique)* |

> **Reejecución del 11-ago-2026 sobre `cdae55e` (lote C ya publicado).** El 11-ago se
> desplegó el lote C y se volvió a correr el recorrido **público** en escritorio y celular.
> Alcance de esa pasada: no ejecuta ningún caso nuevo, sino que **comprueba que el lote C no
> rompió nada** (F-02, F-03, F-04, F-06 y F-07 salen igual que el 03-ago) y **cierra en
> producción D-01 y D-06**. Confirma también que **D-04 y D-11 siguen vivos**, como se
> esperaba: D-11 pertenece al lote A, que aún no se aplica. Todo lo del panel —D-02, D-08,
> D-09, D-10 y el grueso de la tanda F— **queda sin reejecutar**, porque exige una sesión con
> MFA que no se puede automatizar. Evidencia: `SATAG - Evidencia de pruebas/2026-08-11/`.
>
> Al preparar esa pasada hubo que **corregir el arnés**: sus comprobaciones de D-01 y D-06
> afirmaban el comportamiento defectuoso, y la de D-01 además intentaba marcar una casilla que
> ahora llega deshabilitada, lo que **tumbaba la corrida entera**. También se le quitó la fecha
> fija de destino, que hacía que cada regeneración escribiera sobre la carpeta del 03-ago.

> **Reejecución del 17-ago-2026 sobre `4680a05` (lote F ya publicado).** Se desplegó el lote F
> —los siete grupos de la revisión adversaria— y se volvió a correr el recorrido **público** en
> escritorio y celular, más el recorrido del **panel** con sesión MFA. Resultado: **E-07 y E-08
> reverificados** (la firma muestra sus seis elementos; la URL firmada caduca a los 70 s y
> «Volver a abrirla» emite una nueva), **D-01 y D-06 siguen cerrados**, y F-02, F-03, F-04, F-06
> y F-07 salen igual que el 03-ago. D-04 y D-11 siguen vivos, como se espera. Evidencia:
> `SATAG - Evidencia de pruebas/2026-08-17/`.
>
> **Hallazgo de método de esa pasada.** El cliente de Supabase **reintenta con espera creciente**
> ante un fallo de red: cuatro intentos, a los 0.4 s, 1.4 s, 3.4 s y 7.4 s, y no declara el fallo
> hasta unos 8 s. El arnés miraba a los 1.5 s, o sea **dentro de la ventana de reintentos**, y
> anotaba el estado de carga como si fuera el de fallo. Quien leyera esa evidencia habría
> concluido que el formulario se queda en «Cargando…» para siempre. Se corrigió esperando al
> mensaje real. De paso confirma que el parche G1 es correcto: mientras el cliente sigue
> reintentando, decir «Cargando» es la verdad.
>
> **Lo que esa pasada NO cubre y sigue pendiente:** los 27 casos del guion del panel
> (`arnes/guion-tanda-panel.md`), la tanda A completa, la tanda U, P-11, las mitades de pantalla
> de P-03 y P-04, y las mitades de borrado por API de Storage de P-10 y P-13.

> **Sesión de la tanda F del 17-ago-2026 (tarde), sobre `4680a05`.** Se ejecutó el guion del
> panel **completo (27/27)** en modo mixto: Administración, Finanzas y los dos casos de SQL a
> mano por el ejecutor; el bloque de TI y los de solo lectura por **recorrido automatizado con
> la sesión MFA** (`arnes/tanda-f-ti.mjs`, capturas en `salida/tanda-f/`). Extras de la misma
> sesión: **F-32** (las dos mitades, usando `SATAG-000303` sin consumirlo), **P-11** con su
> limpieza inmediata, y la **reejecución de D-08**. Un hallazgo nuevo: **D-15**, detectado por
> el ejecutor humano justo donde el guion esperaba solo confirmación. La tanda F pasa de 5/39 a
> **32/39**; los siete restantes son F-01, F-07 (mitad), F-08 (tras el lote A), F-37 (alta de
> hoy) y las reejecuciones específicas de D-02, D-09 y D-10 que exigen red manipulada.
> Reasignaciones de folio aplicadas y llevadas a la matriz: F-29 absorbe F-09; F-11 → `…103`;
> F-12 → `…104`.

**Resultado:** ✅ Aprobado · ❌ Fallido · ⚠️ Aprobado con observación · ⏭️ No ejecutado

---

## Preparación

| Verificación | Fecha | Resultado | Nota |
|---|---|---|---|
| Banco de QA aplicado (`seed_tests_dev.sql`) | | | |
| Cuentas por rol con MFA inscrito | | | |
| `npx tsc --noEmit` en verde | 31-jul-2026 | ✅ | Sin errores de tipos. |
| `npm run build` en verde | 31-jul-2026 | ✅ | Compila en 1.6 s; las 10 rutas se generan como estáticas (`output: "export"`). |

### Expediente de referencia del 03-ago — `SATAG-000303`

Alta **limpia** por `/registro/`, de **menor de edad con gestionante** y **TAG propio**. Es el primer
expediente del sistema con firma **capturada**, no sembrada, y por eso es el que habilita **E-06,
E-07 y E-08**, que no se pueden probar con los 55 folios del banco de QA.

Se comprobó durante el recorrido que el aviso y el reglamento cargaron con texto real y versión
`v2` antes de aceptar, así que **no** reproduce D-01. El expediente de D-01 sigue siendo el `302`,
que se conserva aparte como evidencia del defecto.

> **Procedencia de la firma, para el acta.** El recorrido se ejecutó con un arnés automatizado
> (Playwright sobre Chromium) que opera la interfaz publicada como lo haría una persona: los trazos
> son eventos de puntero reales, con sus coordenadas y tiempos. **No es la firma de una persona
> física**, y no debe presentarse como tal. Sirve para probar el mecanismo —que la evidencia se
> conserva, se ve en el panel y su URL caduca—, no para acreditar el consentimiento de nadie. Los
> casos de usabilidad (tanda U) siguen exigiendo personal real.

## Tanda P · Privacidad, RLS, RPC y Storage

| Caso | Fecha | Resultado | Evidencia / observación |
|---|---|---|---|
| P-01 | 03-ago-2026 | ✅ | `select * from registros` con la clave anónima devuelve `HTTP 200` y **0 filas**. La RLS no filtra ni un registro: no hay PII al alcance de una sesión anónima. |
| P-02 | 03-ago-2026 | ✅ | Igual sobre las cuatro tablas sensibles: `aceptaciones`, `pagos`, `movimientos` y `solicitudes` devuelven `HTTP 200` y **0 filas** cada una. Ninguna expone lectura a `anon`. |
| P-03 | 03-ago-2026 | ⚠️ | **La mitad de datos, aprobada.** Con rol `admin` pero `aal` en `aal1` —exactamente el estado de quien inició sesión y no completó el TOTP— no se obtiene **nada**: `registros` → 0 filas, `aceptaciones` → 0 filas, el bucket → 0 objetos, las dos vistas → 0 filas, y `registrar_pago` y `cortar_caja` fallan con «Se requiere sesion con segundo factor (MFA)». La frontera no la pone la interfaz: la pone la base. **Falta la mitad de pantalla** —entrar, abandonar en el reto TOTP y comprobar que el panel no pinta ni un dato—, que exige una sesión real y no se puede simular, porque lo que prueba es la cadena de autenticación. |
| P-04 | 03-ago-2026 | ⚠️ | **La mitad de datos, aprobada.** Un autenticado con `aal2` pero **sin** `app_metadata.rol` no obtiene nada: `registros` → 0 filas, `aceptaciones` → 0 filas, el bucket → 0 objetos visibles, y los cuatro RPC del panel rechazan por `panel_exigir_rol`. Ni una consulta devuelve datos, que es lo que el caso exige. **Falta la mitad de pantalla** —que salga «Sin rol asignado»—, que necesita entrar al panel con esa cuenta. |
| P-05 | 03-ago-2026 | ✅ | Con rol `consulta` y `aal2`, los cuatro RPC de escritura fallan por `panel_exigir_rol`, cada uno nombrando el rol que le faltaba: `registrar_pago` → «no tiene el rol requerido (admin)», `instalar_tag_con_estacionamiento` → «(ti)», `dar_baja` → «(ti)», `cortar_caja` → «(admin)». Ninguno llegó a tocar dato. *(De paso: ese mensaje es el tuteo vivo de D-07.)* |
| P-06 | 03-ago-2026 | ✅ | Con rol `ti` y `aal2`, los dos RPC de dinero rechazan por `panel_exigir_rol`: `registrar_pago` → «no tiene el rol requerido (admin)» y `cortar_caja` → «(admin)». En la misma pasada, `instalar_tag_con_estacionamiento` y `dar_baja` **sí** pasan la guardia y fallan sólo por «Registro no encontrado», que es el uuid inexistente de la prueba. La separación está donde debe. |
| P-07 | 03-ago-2026 | ✅ | Con rol `admin` y `aal2`, `instalar_tag_con_estacionamiento` rechaza con «no tiene el rol requerido (ti)», y `dar_baja` igual. En la misma pasada `registrar_pago` pasa la guardia y falla por «Registro no encontrado». Administración cobra y no instala. **Hallazgo incidental:** `cortar_caja(0,…)` pasó la guardia y devolvió «Explique la diferencia de $4200.00 antes de cerrar el corte» — es la lógica de negocio funcionando (hay $4 200 sin cortar en el banco de QA y un efectivo contado de $0 exige observación). Adelanta la comprobación de **F-16**. |
| P-08 | 03-ago-2026 | ✅ | **Rechazado por las dos vías, pero por mecanismos distintos, y conviene que conste cuál protege qué.** `update registros` falla con «permission denied for table registros»: el bloque 30 **revocó el privilegio**, y se confirmó con `has_table_privilege('authenticated','registros','UPDATE') = false`. `insert into pagos` falla con «new row violates row-level security policy for table pagos»: ahí el privilegio **sí existe** (`INSERT = true`), y lo que bloquea es que `pagos` tiene RLS activa y **ninguna** política de insert. Es decir: a `registros` lo defienden dos barreras y a `pagos` sólo una. Ambos mecanismos son independientes del rol del panel —los cuatro roles comparten el mismo rol de base `authenticated`—, así que el resultado vale para todos. **Matiz de método:** el primer intento devolvió «permission denied for sequence pagos_folio_recibo_seq», porque el valor por omisión del folio se evalúa antes que la RLS; hubo que repetir el insert con folio explícito para que la comprobación llegara a la barrera real. Un ejecutor que se quede con el primer mensaje daría por buena una protección que no llegó a probar. |
| P-09 | 03-ago-2026 | ✅ | *(comprueba el bloque 43, aplicado 28-jul)* **Contraprueba hecha el 03-ago:** la lectura pública **no** se rompió — `cat_marcas`, `cat_colores`, `reglamento_versiones` y `aviso_versiones` siguen devolviendo filas a la clave anónima, así que el formulario público carga. **Mitad de escritura, con rol `consulta`:** `insert into cat_marcas` se rechaza con «new row violates row-level security policy», y los `update` sobre `aviso_versiones` y `reglamento_versiones` afectan a **0 filas** — la política del bloque 43 es `for all ... using (admin/super)`, así que a un rol operativo no le hace visible ni una fila para modificar. **Ojo al leerlo:** un `UPDATE` filtrado por RLS **no da error**, termina en silencio afectando cero filas; hay que contarlas con `get diagnostics` para distinguirlo de una escritura consentida. **Repetido con rol `ti` el 03-ago:** mismo resultado — `cat_marcas` rechaza por RLS y los dos documentos dan 0 filas modificadas. **Y la contraprueba con `admin`:** ahí `insert into cat_marcas` **sí se ejecuta** y los dos documentos dan **1 fila modificada** cada uno. Es lo que demuestra que la política del bloque 43 discrimina de verdad por rol y no está negando a todo el mundo por igual, que es el fallo silencioso que este caso podría ocultar. |
| P-10 | 03-ago-2026 | ⚠️ | *(comprueba el bloque 43, aplicado 28-jul)* **Primera mitad cerrada el 03-ago:** el PNG de `SATAG-000303` no es accesible por URL pública directa (`HTTP 400`) ni descargándolo con la clave anónima (`HTTP 400`). El bucket es privado de verdad. **Con rol `consulta`:** lee el bucket (25 objetos visibles) pero **escribir se rechaza** por RLS —«new row violates row-level security policy for table objects»—, que es el reparto del bloque 48: lee, no gestiona. **La mitad de «borrar» no se puede probar por SQL:** `delete from storage.objects` devuelve «Direct deletion from storage tables is not allowed. Use the Storage API», un disparador de Supabase que bloquea el borrado directo **para todos los roles, incluido `admin`**, así que la política `firmas_gestion_admin` nunca llega a evaluarse por esta vía. Para cerrar esa mitad hay que intentar el borrado **por la API de Storage**, que es la que usa el panel. **Completado el 03-ago con los cuatro perfiles:** leer el bucket → `admin`, `ti` y `consulta` ven los 25 objetos, y un autenticado **sin rol** ve **0**. Escribir → `admin` **sí** («SE EJECUTO»), `ti` y `consulta` **no** («new row violates row-level security policy for table objects»). El reparto de los bloques 43 y 48 —los cuatro roles leen, sólo admin/super gestionan— queda verificado salvo el borrado. |
| P-11 | 17-ago-2026 | ⚠️ | *(riesgo aceptado: sin rate limiting)* Ejecutado con `arnes/p11.mjs` **después** de F-27 y F-28, para que sus notas no estorbaran la vinculación. **Los 20 envíos entraron** (HTTP 200, entre 90 y 300 ms cada uno): queda documentado que un script puede meter 20 notas al buzón en ~3 segundos. Se anota como observación y no como fallo porque es la decisión registrada. Limpieza inmediata con `p11-limpieza.sql`: **borradas = 20**, el buzón quedó como estaba. |
| P-12 | 03-ago-2026 | ✅ | Con folio real (`SATAG-000101`) y placas equivocadas, el buzón responde `HTTP 400` con un único mensaje —«Los datos no coinciden con ningún registro vigente»— y **no** revela nombre, vehículo, placas, estado ni tipo de usuario. Comprobado además que un folio **inexistente** da exactamente el mismo mensaje: el buzón no permite distinguir «el folio existe pero la placa está mal» de «el folio no existe», que es lo que impediría usarlo para tantear folios ajenos. |
| P-13 | 03-ago-2026 | ⚠️ | *(bloque 48: `consulta` lee la firma pero no escribe nada)* **Aprobado salvo una mitad que no es comprobable por SQL.** Con rol `consulta`: **lee** la evidencia —`v_evidencia_firma` le devuelve **58 filas**, y **1** al filtrar por `SATAG-000303`— y también ve los 25 objetos del bucket. Y **no escribe nada**: los tres RPC del caso fallan por `panel_exigir_rol` (ver P-05) y el insert en el bucket lo rechaza la RLS. Queda por comprobar el borrado del bucket, que por SQL lo bloquea un disparador de Supabase para todos los roles y hay que probar por la API de Storage (ver P-10). Con eso, la afirmación del bloque 27 —«la separación de `consulta` no es que vea menos, es que no escribe»— queda verificada en las dos direcciones. |
| P-14 | 03-ago-2026 | ✅ | *(vistas `security_invoker`, bloques 45 y 47)* **Primera mitad cerrada el 03-ago:** con `aal2` pero **sin** `app_metadata.rol`, `v_registros_incompletos` y `v_evidencia_firma` devuelven **0 filas las dos**. Con rol sí devuelven datos (12 y 58), así que el cero no es que las vistas estén vacías. **Segunda mitad cerrada el 03-ago:** con rol `admin` pero `aal` en `aal1` —segundo factor sin completar— las dos vistas devuelven también **0 filas**. Es decir: **el rol sin MFA no vale de nada**. Las dos vistas son `security_invoker` (`45:69-70` y `47:84-85`), así que corren con los permisos de quien consulta y heredan la RLS en lugar de abrir una segunda puerta. |

## Tanda E · Evidencia de firma

| Caso | Fecha | Resultado | Evidencia / observación |
|---|---|---|---|
| E-01 | 03-ago-2026 | ✅ | `SATAG-000303` tiene **exactamente una** aceptación (`registro_id` es `unique`), con la ruta `firmas/5b81d559-551d-4695-98d3-c715d22045e8.png` en el bucket privado. |
| E-02 | 03-ago-2026 | ✅ | **Las dos mitades.** El hash del paquete se recalcula solo en la base y cuadra: `hash_documento = encode(digest(convert_to(hash_payload::text,'UTF8'),'sha256'),'hex')` devuelve `true`. Y el PNG descargado por URL firmada (15 113 bytes, `image/png`) recalcula `7ce99a7013e329a4c37d2f6d4eaf872bcb05cf566b2788a1b09c6709ea87ff88`, **idéntico** al `firma_imagen_sha256` almacenado. |
| E-03 | 03-ago-2026 | ✅ | Sobre `SATAG-000303`. El panel muestra «Reglamento aceptado: **Versión 2**» y «Aviso de privacidad aceptado: **Versión 2**», que son las vigentes. Coincide con lo que se vio al firmar: 22 cláusulas y etiqueta «(v2)». |
| E-04 | 03-ago-2026 | ✅ | Sello de tiempo conservado y legible en el panel: «3 de agosto de 2026 a las 12:35:39 p.m.», hora real del envío. |
| E-05 | 03-ago-2026 | ✅ | El panel declara «Trazos vectoriales: **Conservados junto con la imagen**», además del PNG. Confirmado en base: `firma_trazos` no es nulo y `strokes` trae **1** trazo continuo con sus puntos `{x,y,t,p}`, que es la forma del trazo capturado. |
| E-06 | 03-ago-2026 | ✅ | Sobre el alta de menor. El panel muestra «Firmó: **María Fernanda Solís Cárdenas (la madre)**» — el gestionante, no el menor— y en el expediente «Gestionante (paga y firma)» con el mismo nombre. La segunda mitad también queda: la restricción `reg_menor_requiere_gestionante` existe y dice exactamente lo que debe — `usuario_es_menor = false` **o** gestionante con nombres y apellido paterno no vacíos **y** `gestionante_relacion in ('padre','madre','tutor')`. Es decir, la base **no deja** guardar un menor sin representante, ni con un representante de relación «otro». |
| E-07 | 03-ago-2026 | ✅ | *(SC-008, bloque 47)* Con rol `super`, sobre `SATAG-000303`: se muestran **las seis cosas** que pide el caso — la imagen de la firma, quién firmó y en qué calidad, la versión del reglamento, la del aviso, el sello de tiempo y el hash. De hecho muestra dos hashes: el del paquete firmado (`7716ea349d57…5d8599c7`) y el de la imagen (`7ce99a7013e3…ea87ff88`). Es el primer expediente donde la imagen **sí** aparece: los 55 del banco de QA no la tienen porque el seed no escribe en Storage (E-11). |
| E-08 | 03-ago-2026 | ⚠️ | **Aprobado con observación sobre la redacción del caso.** La URL firmada caduca: recién emitida devuelve `HTTP 200` y **pasados 70 segundos devuelve `HTTP 400`**. «Volver a abrirla» emite una URL distinta, que sirve. El panel lo dice en pantalla: «El enlace de la imagen caduca a los 60 segundos». **Pero la otra mitad del esperado está mal escrita:** el caso pide abrir el enlace «en una ventana sin sesión» y dice que «deja de servir en ambos casos». No es así, y no debe serlo: una URL firmada **autoriza por el token, no por la sesión**, así que dentro del minuto abre correctamente sin sesión —se comprobó, `HTTP 200` desde un contexto limpio—. Lo que protege la imagen es la caducidad de 60 segundos, no la sesión. Quien ejecute el caso tal como está escrito hoy lo reprobará por un motivo equivocado. **Corregir el esperado de E-08 en la matriz.** |
| E-09 | 03-ago-2026 | ✅ | Interceptada la respuesta de red al pedir la evidencia: el panel consulta **únicamente** `v_evidencia_firma` y recibe **501 bytes**. No viaja `hash_payload`, ni `firma_trazos`, ni `ip_origen`, ni `user_agent`. Sí viajan los campos probatorios (`hash_documento`, `sello_tiempo`, `firmante_nombre`, `tiene_trazos`). La vista deja fuera lo que el panel no necesita, tal como se diseñó. |
| E-10 | 03-ago-2026 | ⚠️ | *(bloque 48)* **Los cuatro roles ven exactamente la misma evidencia.** `v_evidencia_firma` devuelve **58 filas** a `admin`, a `ti` y a `consulta`, y **1** al filtrar por `SATAG-000303` con los tres; con `super` se abrió el expediente en el panel y se vio la evidencia completa (ver E-07). Ningún rol ve más ni menos que otro. **Riesgo aceptado que se confirma aquí, y se anota como observación, no como fallo:** la RLS de PostgreSQL es por fila, no por columna, así que un `select * from aceptaciones` directo alcanza `hash_payload`, `firma_trazos`, `ip_origen` y `user_agent` con cualquiera de los cuatro roles. La vista `v_evidencia_firma` no los expone y es lo que consume el panel —comprobado en E-09, viajan 501 bytes sin esos campos—, pero la tabla sí los alcanza. Quedó registrado a propósito en `48_evidencia_firma_consulta.sql:31-48`. |
| E-11 | 03-ago-2026 | ✅ | Sobre folios sembrados. `SATAG-000101` muestra firmante, versiones, sello y hash **y además la imagen**: `qa-firma-demo.png` ya está en el bucket, así que la salvedad del caso —«que no cargue la imagen no es fallo»— ni siquiera hizo falta. `SATAG-000225` muestra el estado vacío correcto: «Este expediente no tiene evidencia de firma registrada. Los expedientes dados de alta por el formulario siempre la tienen; si falta, el registro se capturó por otra vía.» |

## Tanda F · Funcional

| Caso | Fecha | Resultado | Evidencia / observación |
|---|---|---|---|
| F-01 | | | |
| F-02 | 03-ago-2026 | ⚠️ | **Cerrado con observación.** La mitad del reglamento, que faltaba, quedó verificada el 03-ago: la casilla llega **deshabilitada**, el recuadro trae **22 cláusulas** y la etiqueta dice «(v2)»; al desplazarse al final la casilla se habilita. Igual en el aviso. Observación de redacción del caso: el esperado dice "el botón permanece deshabilitado", pero lo que se deshabilita es **la casilla** (`registro/page.tsx:454`, `disabled={!avisoLeido}`), y el botón muestra "Debe aceptar el aviso de privacidad para continuar". El propósito —no se puede aceptar sin llegar al final— se cumple. |
| F-03 | 31-jul-2026 | ✅ | La casilla llega **desmarcada y deshabilitada**; se habilita al desplazarse al final (tolerancia de 8 px, `alFinal`). El aviso que dice "Desplácese hasta el final para poder aceptar" desaparece al cumplirse. Es obligatoria: sin ella `validarPaso` bloquea el avance (`:159`). |
| F-04 | 03-ago-2026 | | **Parcial: la parte de pantalla queda cerrada.** Con «menor de edad» marcado, el desplegable trae exactamente `["Seleccione…","Padre","Madre","Tutor"]` — **sin «Otro»**; la casilla de gestionante llega **marcada y deshabilitada** (no se puede desmarcar); «Tipo de usuario» queda en `alumno` y bloqueado. Verificado por recorrido automatizado en escritorio (1280) y celular (390). **Falta** la tercera pata del esperado —que la firma sea del gestionante—, que se comprueba con el alta real junto con E-06. |
| F-05 | 31-jul-2026 | ✅ | Seat → Arona, Ateca, Ibiza, Leon, Tarraco, Toledo, Otro: los modelos corresponden a la marca. Marca «Otro» abre captura libre de marca y de modelo. La rama modelo = «Otro» se verificó en el código, no en pantalla. Observación menor: «Leon» debería llevar acento en el catálogo. |
| F-06 | 03-ago-2026 | ✅ | Con el paso vacío salen **los cuatro** mensajes, uno por campo: «Seleccione o escriba la marca.», «…el modelo.», «…el color.» y «Capture las placas o marque «sin placas».». Con placa mal formada (`AB1`) sale «Formato de placa no válido (5–8 letras o números).». En ambos casos el asistente **no avanza** y nada se guarda: el envío ni siquiera se intenta, la validación es previa (`validarPaso`). Verificado en escritorio y celular. |
| F-07 | 03-ago-2026 | | **Parcial.** El chip «Ya tengo TAG propio» activa y muestra el aviso "El registro y la activación de un TAG propio tienen el mismo costo ($100); llévelo el día de la instalación", que es la prueba en pantalla del acuerdo de CC-01. **Falta** el segundo registro con TAG de la escuela para comparar ambas procedencias en la base. |
| F-08 | | | |
| F-09 | 17-ago-2026 | ✅ | **Absorbido por F-29** (los dos pedían `…101` sin pago; ejecutar uno destruía la precondición del otro). El cobro de `…101` generó el folio de recibo **automático `SATAG-2026-000043`** con el formato `SATAG-AAAA-######`, y los cuatro cobros del día salieron correlativos (…043, …044, …045, …046). El expediente pasó a la cola de instalación de TI. *(La nota anterior de esta fila —«B5 no conectado»— estaba obsoleta: el bloque 46 está aplicado desde el 29-jul.)* |
| F-10 | 17-ago-2026 | ✅ | Sobre `…101` ya cobrado: el panel **no ofrece** volver a cobrar —solo queda «Ver la firma»—, el chip dice «Pagado» y no se generó segundo folio. Captura con el sello completo del expediente. |
| F-11 | 17-ago-2026 | ⚠️ | **Aprobado con observación de método.** Reasignado a `…103` (su folio original, `…102`, chocaba con F-30). El resultado observable es correcto: **un solo pago, un solo folio** (`SATAG-2026-000045`). Pero el doble clic humano no le ganó al render —el diálogo se cierra antes del segundo clic—, así que **el candado síncrono anti-doble-RPC no llegó a ejercitarse**: se probó que un clic produce un pago, no que dos produzcan uno. Si el auditor lo exige, se ejercita por código con el arnés disparando los dos clics sin espera. |
| F-12 | 17-ago-2026 | ✅ | Reasignado a `…104` (corrimiento por F-11). Cobro con monto editado a **$150.00**: se guardó el monto capturado, no el predeterminado, con recibo `SATAG-2026-000046`, visible en el historial de Finanzas con fecha, expediente y quién cobró. |
| F-13 | 17-ago-2026 | ✅ | La cola de Administración encabeza con los pendientes **sin pago**; pagados y bajas se distinguen por chip. Verificado antes del primer cobro del día. |
| F-14 | 17-ago-2026 | ✅ | **La aritmética cuadra completa:** «En caja ahora» $4,650.00 = 46 cobros no cortados (1 del 20-jul $100 + 41 del 30-jul $4,100 + 4 de hoy $450). «Vendido este mes» $450 (solo los de hoy: los demás son de julio) y el histórico $4,650 coincide con el total. El desglose por día de cobro además avisa: «Esta caja mezcla cobros de 3 días…». |
| F-15 | 17-ago-2026 | ✅ | Cobro nuevo de `…192` ($100), corte con efectivo contado **exacto**: las observaciones fueron **opcionales** esta vez, el corte `SATAG-CORTE-2026-000002` quedó sellado con diferencia «—», la identidad de quien cortó y la caja en ceros. El contraste con F-16 demuestra que la observación se exige **solo** cuando hay diferencia. |
| F-16 | 17-ago-2026 | ✅ | **Las dos mitades.** Con $450 contados contra $4,650 esperados, el cierre se **bloqueó** con «El efectivo no cuadra: explique la diferencia antes de cerrar» y el campo de observaciones pasó a obligatorio en rojo. Con la explicación escrita, selló `SATAG-CORTE-2026-000001` con **diferencia −$4,200.00 registrada**, la observación completa y quién cortó. El diálogo previo avisa que el corte no se podrá modificar. |
| F-17 | 17-ago-2026 | ✅ | **Las dos vías fallan.** Panel: no existe botón de editar ni borrar un corte (solo expandir). SQL: los cuatro intentos dentro de `begin…rollback` reventaron con su mensaje propio — `update cortes_caja` y `delete from cortes_caja` → «Un corte de caja cerrado no se puede modificar ni borrar (folio SATAG-CORTE-2026-000002)» con el HINT «deje constancia en las observaciones del corte siguiente»; `update pagos` → «El pago SATAG-2026-000001 ya fue cortado y no admite cambios»; `delete from pagos` → «…pertenece a un corte de caja cerrado y no se puede borrar». El blindaje del bloque 42 completo. |
| F-18 | 17-ago-2026 | ✅ | El detalle de `SATAG-CORTE-2026-000001` lista **exactamente los 46 cobros** sellados en él, con fecha, recibo, expediente, monto y quién cobró; el de `…000002`, exactamente su único cobro (`…192`). |
| F-19 | 17-ago-2026 | ✅ | *(recorrido automatizado con la sesión MFA — `arnes/tanda-f-ti.mjs`)* Instalación de `…111` con TAG `7100111` y acceso E1: el modal resume **vehículo, titular, acceso y número** antes de confirmar («Se instalará el TAG 7100111 en el Volkswagen Jetta Azul (ABC1211) de Elena Torres Vega, con acceso a E1…»), y tras confirmar el expediente **salió de la cola de instalación**. |
| F-20 | 17-ago-2026 | ✅ | *(automatizado)* `…191` aparece en la sub-sección atenuada **«Esperando pago»** y su tarjeta **no ofrece el formulario de instalación** (cero campos de TAG). |
| F-21 | 17-ago-2026 | ✅ | *(automatizado)* Sobre `…112`: el formato corto se rechaza en pantalla («Lleva 3 dígitos; deben ser de 6 a 11») y el duplicado lo rechaza **el servidor** con mensaje que nombra al dueño: «El TAG 7000121 ya esta activo en otro registro (SATAG-000121)». `…112` quedó sin TAG, intacto. |
| F-22 | 17-ago-2026 | ✅ | *(automatizado)* Al atender la solicitud de `…141` editando placas, el modal **resume el cambio antes de confirmar**: «Cambios en SATAG-000141 (Rosa Mendez Paz): placas ABC1241 → UAB9141. ¿Guardar?». Tras confirmar, la solicitud quedó atendida y el expediente salió de la cola. |
| F-23 | 17-ago-2026 | ✅ | *(automatizado)* En la baja de `…151` el motivo llegó **prellenado desde la solicitud** («Egreso del alumno al terminar el ciclo»); tras confirmar, salió de las colas activas. |
| F-24 | 17-ago-2026 | ✅ | *(automatizado + juicio visual sobre la captura)* Las peticiones más antiguas encabezan la cola (28 días > 18 días) y el badge de espera cambia de color por antigüedad. Captura `tanda-f/01-F24-cola-instalar.png`. |
| F-25 | 17-ago-2026 | ✅ | *(automatizado)* En `…211`, «Usar el TAG apartado» presente y funcional: el modal explica la reposición completa («Se activará el TAG apartado 9426781… El TAG actual 7000211 quedará inactivo y la procedencia pasará a escuela»), y la tarjeta quedó `TAG 9426781`, activo, con sus accesos. CC-01 cerrado en pantalla. |
| F-26 | 17-ago-2026 | ✅ | *(automatizado)* Al ejecutar la actualización que pedía la nota vinculada de `…171`, la nota **se cerró sola** (bloque 38): el expediente salió de la cola sin tocar la nota. |
| F-27 | 17-ago-2026 | ✅ | *(automatizado)* La nota de Paula Nieto se vinculó a su gemelo `…201` buscando por nombre; el trámite llegó **precorroborado en lo que pidió el cliente** («Actualizar datos») y el modal lo dice completo: «Se vinculará la nota de Paula Nieto al expediente SATAG-000201 (Paula Nieto Cruz) y aparecerá en “Actualizar datos”». Tras confirmar, **la app navega sola** a la cola del trámite corroborado con el expediente abierto y la nota colgada encima del formulario (bloque 39). Captura `tanda-f/01-F27-vinculada.png`. |
| F-28 | 17-ago-2026 | ✅ | *(automatizado)* Nota de Victor Tello descartada sin vincular, con motivo capturado: se cerró sin error y desapareció de la cola (bloque 36). |
| F-29 | 17-ago-2026 | ✅ | *(CC-05, bloque 46; absorbe F-09)* Cobro de `…101` **dejando el tipo tal como llegó**: el diálogo previo declara quién cobra y cómo quedará validado; el expediente quedó «padre/madre/tutor · **validado** · Gerardo Sanchez · 17 ago 2026», y **cero movimientos escritos** — verificado por consulta: el banco de QA no siembra movimientos, así que la ausencia es la prueba exacta de «no hubo corrección que registrar». |
| F-30 | 17-ago-2026 | ✅ | Cobro de `…102` **corrigiendo** el tipo (venía como *maestro*): el diálogo avisó del cambio **antes** de cobrar, y quedó **exactamente un** movimiento `cambio` — «Tipo de usuario: maestro -> admin (validado al cobrar) · Gerardo Sanchez». El contraste con las cero filas de F-29 demuestra el bloque 46 en las dos direcciones. |
| F-31 | 17-ago-2026 | ✅ | *(SQL Editor, patrón de la tanda P: claims de admin+aal2, todo en `rollback`)* `registrar_pago` **sin** `p_tipo_usuario` falló con «Confirme el tipo de usuario antes de cobrar (maestro, padres, alumno o admin)» y **ningún pago quedó registrado**. |
| F-32 | 17-ago-2026 | ⚠️ | **Las dos mitades pasan, con observación de usabilidad (→ D-15).** SQL (mismo patrón que F-31): cobrar al menor `…303` como 'maestro' falló con «El titular es menor de edad: su tipo debe ser alumno». Panel: el formulario de cobro de `…303` explica «El titular es menor de edad: su tipo queda fijo en alumno y firma su padre, madre o tutor» y **solo deja seleccionar Alumno** — se verificó y se **canceló sin cobrar** para conservar el 303 sin pago. **Observación:** los chips de los demás tipos **se ven habilitados** (mismo color y cursor que uno pulsable) y al hacer clic no pasa nada; el propio ejecutor pensó que algo fallaba antes de leer el texto. La restricción funciona; la señal visual la contradice. |
| F-33 | 17-ago-2026 | ✅ | Contraste directo entre expedientes: `…101` (cobrado hoy) muestra «**validado** · Gerardo Sanchez · 17 ago 2026»; `…121` (cobro sembrado del 30-jul) muestra «**sin validar**» y su pago «Sin responsable». No hubo relleno retroactivo, exactamente como pide el caso. |
| F-34 | 17-ago-2026 | ✅ | *(automatizado)* Los seis folios `…221`…`…226` aparecen en el reporte, cada uno con su motivo; `…226` con **dos** motivos a la vez («TAG instalado sin pago» y «Sin placas capturadas»). |
| F-35 | 17-ago-2026 | ✅ | *(automatizado)* El expediente sano `…121` **no** aparece en el reporte. |
| F-36 | 17-ago-2026 | ✅ | *(automatizado)* Las bajas `…131`…`…134` **no** aparecen, aunque les falte de todo. |
| F-37 | | | *(pendiente: necesita un alta hecha HOY sin pago; sale de la prueba de campo)* |
| F-38 | 17-ago-2026 | ✅ | *(automatizado)* Cada faltante trae su etiqueta y el expediente dice quién lo resuelve; `…226` dice literalmente «Lo resuelve: **Administración y TI**». |
| F-39 | 31-jul-2026 | ✅ | Las tres patas. **Plegado:** se ven responsable, finalidades y el enlace al integral. **Desplegado:** aparecen los otros dos párrafos, el botón cambia a «Ocultar el aviso» y el enlace al integral no se pliega. **Paso 3:** no hay ningún enlace a la página pública; el texto íntegro está ahí mismo. Observación de usabilidad: «Leer el aviso completo» sólo se subraya en `:hover` y en celular no hay hover, así que no se lee como pulsable. |

## Tanda A · ARCO y ciclo de vida

| Caso | Fecha | Resultado | Evidencia / observación |
|---|---|---|---|
| A-01 | | | |
| A-02 | | | |
| A-03 | | | |
| A-04 | | | |
| A-05 | | | |
| A-06 | | | |
| A-07 | | | *(se espera hallazgo → SC-007)* |
| A-08 | | | |

## Tanda U · Usabilidad

| Caso | Fecha | Participante (rol, no nombre completo) | Resultado | Observación |
|---|---|---|---|---|
| U-01 | | | | |
| U-02 | | | | |
| U-03 | | | | |
| U-04 | | | | |
| U-05 | | | | |

---

## Defectos encontrados

| # | Caso origen | Descripción | Severidad | Estado | Corregido en |
|---|---|---|---|---|---|
| D-01 | F-02 / F-03 | **La puerta del consentimiento se abre sola si el aviso no carga.** Las cinco cargas iniciales no tienen `.catch` (`registro/page.tsx:71-77`); si `getAvisoVigente()` falla, `aviso` queda en `null` y el recuadro pinta un solo párrafo, «Cargando…» (`:443`). Ese párrafo cabe en los `max-height: 220px` de `.reglamento` (`globals.css:154`), así que se cumple `scrollHeight <= clientHeight + 8` y el efecto ejecuta `setAvisoLeido(true)` (`:93`). La casilla se habilita y el alta viaja con `aceptaPrivacidad: true`, `avisoLeido: true` y `avisoVersion: null`. Queda constancia en `aceptaciones` de que la persona leyó y aceptó un aviso vacío. El efecto sólo pone la bandera en `true`, nunca en `false`, así que si el texto llega tarde la puerta ya se abrió. Lo mismo con el reglamento en `:97`. **Reproducido el 31-jul** bloqueando `aviso_versiones` y `reglamento_versiones` desde el panel Request conditions: el recuadro mostró «Cargando…» y el aviso «Desplácese hasta el final para poder aceptar» desapareció, lo que confirma `avisoLeido = true`. Afecta **a los dos documentos**: el paso 4 mostró «1. Cargando…» con la casilla habilitada y la etiqueta «…reglamento de acceso vehicular **(v—)**» (`:478`, versión vacía). **Se completó el alta en ese estado y quedó el folio `SATAG-000302`.** Agravante descubierto al revisarlo: cuando el cliente no manda las versiones, `crear_registro` **las resuelve él mismo del lado del servidor** (`19_rpc_crear_registro.sql:81-89` y `:100-108`), escribe `acepto_privacidad` en duro como `true` (`:256`) y calcula el hash sobre el texto completo de la v2 (`:220-226`). Es decir: `aceptaciones` afirma que la persona leyó y aceptó reglamento v2 y aviso v2, con hash válido y sello de tiempo real, **cuando en pantalla no cargó ni un carácter de ninguno de los dos**. E-02 y E-03 aprobarían ese registro: el hash cuadra y las versiones son las vigentes. **Sí queda rastro, pero nadie lo mira:** `metadata->'consentimiento'` conserva lo que reportó el cliente, y ahí `avisoVersion` y `reglamentoVersion` valen `null`, en contradicción con las claves foráneas que apuntan a la v2. Esa contradicción sirve como control de auditoría, siempre que se filtre por `jsonb_exists(metadata,'consentimiento')`: los 55 folios del banco de QA se insertan directo en la tabla y no traen esa clave (`seed_tests_dev.sql:386`), así que sin el filtro salen como falsos positivos. Verificado el 31-jul: **el único expediente afectado es `SATAG-000302`**. **Reejecutado el 11-ago contra el sitio publicado (`cdae55e`), en escritorio y celular:** con `aviso_versiones` y `reglamento_versiones` bloqueados, el recuadro muestra el error en rojo en lugar de «Cargando…», la casilla llega **deshabilitada**, el aviso «Desplácese hasta el final» **ya no aparece** —no hay nada que desplazar— y «Siguiente» no avanza: responde «No se puede continuar porque el aviso de privacidad no se ha cargado correctamente». El paso del reglamento ni siquiera es alcanzable, porque su puerta se cierra por la misma vía. Evidencia en `SATAG - Evidencia de pruebas/2026-08-11/`. **Falta la mitad de servidor:** `crear_registro` sigue resolviendo las versiones por su cuenta si cualquier otro cliente las manda nulas; va en el bloque 49. | **Crítica** | Cliente **corregido y reejecutado**; servidor abierto (bloque 49) | `25b607e` + `6364ff0` |
| D-04 | F-39 / A-07 | **El aviso simplificado del paso 1 desaparece por completo y en silencio si no carga.** Todo el recuadro es condicional (`registro/page.tsx:244`) y `getAvisoSimplificado` devuelve `null` sin avisar (`api.ts:97`). Reproducido el 31-jul: el paso 1 quedó sin ningún rastro del aviso, sin hueco ni mensaje. Es el mínimo del art. 16 fr. II —informar al recabar— cayéndose sin que nadie se entere; el bloque 44 se aplicó justo para cubrirlo (SC-007/CC-09). | Mayor | Abierto | |
| D-05 | F-08 | El área que instala el TAG se llama de dos maneras en el mismo flujo: la portada dice «**Sistemas** lo instala» (`page.tsx:22`) y el comprobante del paso 6 dice «**TI** instalará y activará su TAG». El Manual del Usuario usa «Sistemas» 21 veces y reserva «TI» para lo interno, así que el comprobante es el que se sale. En el mismo renglón del comprobante, «preséntese en **administración**» va en minúscula. **Ampliado el 03-ago** con tres casos más, todos de cara a alguien de fuera: el buzón público dice «Sistemas **(TI)** del IAQ» (`solicitudes/page.tsx:139`), único punto de esa página que expone la sigla interna —las otras cuatro menciones dicen «Sistemas» a secas—; la pantalla de acceso al panel, a la que se llega por URL pública, dice «Acceso para personal de **administración** y **TI** del IAQ» (`admin/page.tsx:177`), con los dos errores juntos; y la confirmación de cobro dice «El estacionamiento y el TAG los asigna **TI** después de confirmar este pago» (`VistaAdmin.tsx:290`), que es la frase que la cajera lee en voz alta a la familia. Dentro del panel «TI» es correcto y se queda: lo que hay que corregir es donde el texto sale a un lector externo. | Menor | **Corregido** (lote A, 17-ago); falta reejecutar F-08 | `7caa24f` |
| D-08 | F-18 / F-14 | **En Finanzas, un detalle de corte que no carga se pinta como un corte sin movimientos.** El `catch` de `toggleDetalle` (`VistaFinanzas.tsx:137`) guarda una lista vacía cuando `listPagosDeCorte` falla, y `DetalleCobros` la renderiza con el texto «Sin cobros en este periodo» (`:42`). No hay forma visible de distinguir «este corte no tuvo cobros» de «la consulta se cayó». El comentario de `:135-136` lo llama «degradación suave», pero es la pantalla del dinero: quien concilia puede dar por bueno un corte vacío y firmar un faltante que no existe. Mismo patrón que D-04, sobre el módulo contable. **Detectado por revisión de código el 03-ago; pendiente de reproducir** bloqueando la consulta desde el navegador. **Código corregido el 07-ago** (`94f4ee4`): error por fila en rojo con `role="alert"`, mensaje real de la API, instrucción de no conciliar y botón Reintentar; el análisis destapó además que el `[]` cacheado bloqueaba el reintento para siempre. **Reejecutado el 17-ago** en la sesión de la tanda F: el detalle de la caja con 46 cobros y el de los dos cortes nuevos abren completos, con fecha, recibo, expediente, monto y quién cobró; en toda la sesión no apareció ni un «Sin cobros» falso. | **Mayor** | **Corregido y reejecutado** | `94f4ee4` |
| D-09 | F-19 | **Si el catálogo de estacionamientos no carga, la pantalla de TI se inventa dos claves.** `VistaTi.tsx:145` hace `.catch(() => setEstacionamientos(["E1", "E2"]))`: ante un fallo de red, TI ve dos chips que parecen el catálogo real y no hay aviso de que sean un respaldo. Si asigna con ellos, o el RPC rechaza la clave inexistente —con un error que no explica la causa— o acierta por casualidad y deja fuera los cajones que sí existían. Es un estado inválido con apariencia de válido, en el acto que F-19 declara «una sola transacción». **Detectado por revisión de código el 03-ago; pendiente de reproducir.** **Código corregido el 07-ago** (`2a0839f`): se quitó el respaldo inventado y el catálogo caído se declara en pantalla. **Sin reejecutar:** exige sesión con MFA. **Grupos G5 y G6 de la revisión, verificados y CORREGIDOS el 17-ago (`ad11d9a`):** G6 era cierto —el estado arrancaba en `[]`, así que «cargando» se veía igual que «cargó vacío» y el formulario pedía elegir de una lista vacía sin decir por qué—; ahora hay tres estados (`undefined` cargando, `null` no cargó, `[]` vacío). **No** era regresión de este parche: el `[]` es anterior. G5 también era cierto —el botón de instalar seguía habilitado con el catálogo caído si el expediente ya traía asignación— pero **su remedio propuesto estaba mal**: deshabilitarlo habría dejado a TI sin poder instalar un acceso ya bien asignado, porque esas claves vienen de la base y no están inventadas. Lo que mentía era el mensaje, y es lo que se corrigió. | **Mayor** | **Corregido y reejecutado** (17-ago, sonda `arnes/sondas-red.mjs` con la red del catálogo cortada: durante los reintentos se ve «Cargando el catálogo de estacionamientos…» —el estado nuevo de G6— con el botón deshabilitado y visiblemente gris (D-15 recién desplegado), y al rendirse declara «No se pudo cargar el catálogo… y este expediente no tiene ninguno asignado» —el mensaje honesto de G5— sin un solo chip inventado. Una sonda cerró D-09 y la reejecución en producción de G5 y G6.) | `2a0839f` |
| D-10 | P-03 / P-04 | **El panel puede quedarse en «Verificando sesion…» para siempre.** `app/admin/page.tsx:48` llama a `supabaseAuth.auth.getSession()` sin `.catch`. Es la única llamada que saca a la pantalla del estado de verificación: si la promesa se rechaza —red caída, token corrupto en el almacenamiento local— `setVerificando(false)` nunca corre y el Loader de `:119` se queda fijo, sin mensaje de error y sin camino al formulario de acceso. La persona no puede ni entrar ni enterarse de por qué. Afecta la ejecución de P-03 y P-04, que se prueban justo en esa pantalla. **Detectado por revisión de código el 03-ago; pendiente de reproducir.** **Código corregido el 07-ago** (`7cb9b01`): además del `.catch`, un tope de 10 s sobre todo el arranque con el aviso «No se pudo restaurar su sesión anterior…», y `refrescarMfa` diferido fuera del callback de `onAuthStateChange`. **Sin reejecutar:** exige sesión con MFA. **Grupos G3 y G4 de la revisión, verificados y CORREGIDOS el 17-ago (`5ff88ec`):** los dos eran reales y compartían raíz — `Promise.race` abandona la carrera pero **no cancela** `restaurar()`, y nada marcaba como vieja la lectura del AAL que quedaba en vuelo. G3: el `.catch` borraba `email` y `rol` aunque `getSession` ya hubiera devuelto una sesión válida y lo colgado fuera solo la lectura del AAL, mandando al formulario de acceso a alguien autenticado sin vuelta atrás, porque `INITIAL_SESSION` ya había disparado. G4 era el grave: una lectura vieja que devolvía `aal2` tras el tope dejaba `mfaOk` en `true` con `email` en null, y al entrar luego con contraseña el render `email && !mfaOk` daba false y **montaba el panel saltándose `GateMfa`**. La base aguantaba sola —P-03 probó que con `aal1` las consultas devuelven cero filas y los RPC rechazan—, así que era un hueco de interfaz y no una fuga; aun así mostraba el panel a quien no pasó el segundo factor. Ahora cada lectura del AAL toma un folio y solo la vigente escribe, y `GateMfa` sube el folio al verificar. | **Mayor** | **Corregido y reejecutado** (17-ago, sonda con la red de auth cortada, por las dos piernas: con token vencido cae al formulario con «No se pudo restaurar su sesión anterior…» a los ~14 s; con token fresco el panel restaura completo, porque el nivel MFA se decodifica localmente del JWT sin salir a la red. Ninguna pierna se queda en «Verificando sesión…». Ese segundo dato acota además a G3: su ventana exacta —sesión restaurada por red + AAL colgado— es más estrecha de lo que modeló la revisión; queda verificado en código (`5ff88ec`) y ninguna pierna observable tira la sesión.) | `7cb9b01` |
| D-11 | A-07 / F-39 | **La página pública del aviso se pinta vacía y con apariencia de correcta.** `getAvisoVigente` arma `parrafos` filtrando líneas en blanco y **no lanza** si el resultado queda vacío (`api.ts:76-79`); `aviso-de-privacidad/page.tsx:40-44` lo vuelca sin comprobar. Con un `contenido` en blanco o sólo espacios, el visitante ve el encabezado y la línea «Versión N del aviso, vigente al momento de esta consulta» sobre un cuerpo **sin una sola cláusula**, sin error ni hueco. Es la URL que la propia base publica en `url_publica` y a la que remiten el formulario y la portada: el aviso integral podría estar caído y la página seguiría afirmando que está vigente. Hermano de D-04 y del mismo linaje que D-01: la evidencia de cumplimiento se sostiene sola cuando el contenido falta. **Reproducido el 03-ago** inyectando una respuesta con `contenido` en blanco: la página quedó con **cero párrafos** y siguió afirmando «Versión 2 del aviso, vigente al momento de esta consulta», sin error ni hueco. Un visitante no tiene forma de saber que está viendo un aviso vacío. | **Mayor** | **Corregido** (lote A, 17-ago); falta reejecutar el fallo inyectado del arnés | `7caa24f` |
| D-13 | F-02 / F-39 / A-07 | **El aviso de privacidad publicado no lleva un solo acento.** `22_publicar_aviso_v2.sql` y `44_aviso_simplificado.sql` tienen **cero** caracteres acentuados en todo el archivo: el texto que la persona lee y acepta dice «Adquisicion», «Instituto Asuncion de Queretaro», «transferira», «informacion», «supresion». No es un detalle de código: es **el documento legal** que el titular acepta con su firma y el que Direccion y Legal van a aprobar y a publicar como aviso institucional. El contraste lo hace peor de dos maneras. Dentro de la misma página del aviso, el encabezado escrito en React sí acentúa («Tratamiento de datos personales del Sistema de **Adquisición** de TAG Vehicular… Instituto **Asunción** de **Querétaro**») y el cuerpo que viene de la base, no. Y en el asistente, dos pasos seguidos: el reglamento (`23_publicar_reglamento_v2.sql`, 22 líneas acentuadas) está bien escrito y el aviso del paso anterior no. **Corregirlo obliga a tocar `aviso_versiones.contenido`, lo que fuerza a resembrar el banco de QA** (`seed_tests_dev.sql:352` guarda el sha256 del texto): conviene que viaje en la misma v3 que apruebe Legal, junto con el plazo de conservación y las opciones de limitación de uso, y no como cambio suelto. | **Mayor** | Abierto | |
| D-14 | F-02 / U-01 | **En celular, el consentimiento se otorga mirando por una mirilla.** `.reglamento` fija `max-height: 220px` (`globals.css:154`), así que en un teléfono de 390 px el aviso integral completo y las 22 cláusulas del reglamento se leen por una ventana de unos cinco renglones, con media pantalla vacía por encima. Nadie lee un documento legal así: la puerta de «desplácese hasta el final» se convierte en un trámite de arrastrar el dedo, que es justo lo contrario de lo que la puerta pretende probar. Además **es lo que hace explotable a D-01**: cuanto más pequeño es el recuadro, más fácil es que el párrafo «Cargando…» quepa entero y dispare `scrollHeight <= clientHeight + 8`. Un recuadro alto no arregla D-01, pero le quita la mecha. Se anota junto al lote B, que ya toca el ancho en celular. | Menor | Abierto | |
| D-12 | F-08 | **Seis textos de cara al usuario van sin acentos**, en las dos pantallas de entrada al panel, conviviendo con JSX vecino que sí acentúa: «No se pudo iniciar sesion.» (`app/admin/page.tsx:81`), «…recibira un enlace para restablecer su contrasena…» (`:93`), «Verificando sesion…» (`:119`), «No se encontro el factor verificado.» (`GateMfa.tsx:54`), «No se pudo iniciar la verificacion.» (`:68`) y «No se pudo verificar el codigo.» (`:83`). En la misma pantalla, `page.tsx:193` dice «Iniciar sesión» y `GateMfa.tsx:155` «Verificación en dos pasos», así que la inconsistencia se ve de un vistazo. | Menor | Abierto | |
| D-02 | Pantalla 2 | Al marcar «El conductor es menor de edad» y **desmarcarla**, «Tipo de usuario» se queda en **Alumno** en lugar de volver al predeterminado, sin aviso visual (`registro/page.tsx:295`). Mitigado aguas abajo: el bloque 46 obliga a Administración a confirmar el tipo al cobrar. **Corregido el 07-ago (`dd9260b`) y reejecutado el 17-ago con sonda automatizada** (`arnes/sondas-red.mjs`): padres → marcar menor deja `alumno` bloqueado → al desmarcar **vuelve a `padres` y el selector se desbloquea**. | Menor | **Corregido y reejecutado** | `dd9260b` |
| D-03 | F-08 | **Cinco** textos tutean dentro del asistente: «Especifica la marca» (`registro/page.tsx:371`), «Especifica el modelo» (`:388`), «Especifica el color» (`:403`), **«Desplázate hasta la cláusula 22 para poder aceptar»** (`:474`) y **«Puedes firmar con el dedo (táctil) o con el mouse»** (`:492`, paso 5). El de `:474` es el más visible: su gemelo del paso anterior (`:447`) sí dice «Despláce**se** hasta el final», así que dos pantallas seguidas tratan al usuario de distinto modo. En la pantalla del vehículo el `:379` dice «Escriba el modelo». Viola CC-07. El Manual del Usuario los cita textualmente en las líneas 103, 107 y 109, así que se corrigen en el mismo cambio. **Ampliado el 03-ago:** el quinto (`:492`) apareció al entrar al paso 5, que el barrido del 31-jul había excluido de su alcance. | Menor | **Corregido** (lote A, 17-ago); falta reejecutar F-08 | `7caa24f` |
| D-06 | F-01 / E-05 | **La firma sobrevive a «Atrás» y se envía un PNG que la pantalla ya no muestra.** `retroceder()` (`registro/page.tsx:173-176`) no toca `firma` ni `trazos`, pero `SignaturePad` se desmonta al salir del paso 5 y al volver se remonta **en blanco**: su único efecto de montaje (`SignaturePad.tsx:30-45`) reasigna `canvas.width`/`canvas.height`, lo que borra el lienzo, y `strokes` es un `useRef` que vuelve a nacer vacío. El padre, en cambio, conserva el PNG y el vector anteriores. Resultado: la persona firma, pulsa «Atrás» para releer una cláusula, regresa, ve **el recuadro vacío** y `validarPaso(4)` (`:161`) la deja enviar igual, porque sólo comprueba `!firma`. Se sube la firma de la pasada anterior. La evidencia queda válida en hash y sello, pero **no corresponde a lo que la persona tenía delante al pulsar «Enviar registro»**, que es justo lo que la firma debe probar. Detectado el 31-jul y aplazado por estar el paso 5 fuera del alcance de aquel barrido; **entra hoy al recorrer los pasos 4 a 6**. **Reproducido el 03-ago en escritorio y celular** con el recorrido automatizado: tras «Atrás» + «Siguiente», el lienzo queda con todos sus píxeles transparentes y el botón «Enviar registro» sigue activo. La captura del lienzo posterior al regreso es **byte a byte idéntica** a la del lienzo nunca usado (84 153 bytes en escritorio, 131 509 en celular). El arreglo correcto no es limpiar la firma al retroceder —eso destruye evidencia válida ante cualquier «Atrás» accidental— sino repintar: `trazos` ya sube al padre, falta pasarlo como prop y redibujar los trazos en el efecto de montaje. **Corregido así el 07-ago** (`a1c9efe`: prop `trazosIniciales`, reescalado al ancho actual del lienzo, `t0` monótono y sin reemitir al padre) **y reejecutado el 11-ago contra el sitio publicado**, en escritorio y celular: tras «Atrás» + «Siguiente» el lienzo **ya no queda vacío**, se repinta a partir del vector de trazos. Evidencia en `SATAG - Evidencia de pruebas/2026-08-11/`. **Grupo G7 de la revisión adversaria, verificado y CORREGIDO el 17-ago (`21fd797`):** era real y peor de lo descrito. El recuadro es `width: 100%` con `height: 200px` **fijo** (`globals.css:159-162`), así que la escala vertical siempre valía 1 y la horizontal se movía con el ancho: girar el teléfono de vertical a horizontal estiraba los trazos más del doble a lo ancho sin tocar el alto, y bastaba añadir un trazo más para que `emitirTrazos` reemitiera la versión deformada y **sustituyera la evidencia fiel**. Ahora se usa una sola escala uniforme, la menor de las dos. Se cerró además la variante de destrucción: el guardia comprobaba `previa.width` pero no `rect`, así que con el lienzo aún sin medir todos los puntos colapsaban al origen. | Mayor | **Corregido y reejecutado** | `a1c9efe` |
| D-07 | F-08 | **El tuteo también viaja desde la base de datos.** `apiPanel.ts:46-47` deja escrito que los errores de negocio de los RPC «se muestran tal cual», y `api.ts:223` y `:254` reenvían `error.message` sin tocarlo, así que el texto del `raise exception` llega literal a la pantalla. Tutean **cuatro cadenas vivas**, en dos funciones: `crear_solicitud` (`28_rpc_crear_solicitud.sql`, bloque 28) con «**Describe** brevemente que **necesitas**» (`:56`), «**Captura tu** folio y **tus** placas (o No. de TAG)» (`:65`) y «…en proceso para **tu** registro» (`:90`); y `panel_exigir_rol` (`29_rpc_panel.sql:53`) con «**Tu** usuario no tiene el rol requerido». Las tres primeras salen en el **buzón público con folio**, cara al usuario; la cuarta en el panel. Se suma «**Debes** aceptar el reglamento» (`api.ts:163`), del cliente. Viola CC-07 en una capa que F-08 no alcanza, porque ese caso acota el recorrido al asistente. **Verificado el 03-ago que el camino de la nota sin folio NO está afectado:** los ficheros `34_buzon_notas_sin_folio.sql:95` y `35_notas_generalizar_solicitante.sql:96` también tutean, pero son código muerto — el bloque 41 reescribió `crear_nota_solicitud` en usted («Falta su nombre», `:55`) y los bloques 35 y 37 hicieron `drop function` de las firmas viejas, así que esas cadenas ya no existen en la base. Quien haga el arreglo debe corregir **sólo** los bloques 28 y 29. Ninguna de las dos funciones cambia de firma, así que basta `create or replace` del cuerpo: **sin `drop function` ni `notify pgrst`**. Va en el **bloque 49**, junto con el arreglo de D-01. | Menor | **Cliente corregido** (lote A, 17-ago); las 4 cadenas de la base van en el bloque 49 | `7caa24f` |
| D-15 | F-32 | **Los tipos de usuario bloqueados se ven pulsables.** En el cobro de un menor de edad, el formulario fija el tipo en *Alumno* y lo explica en texto («El titular es menor de edad: su tipo queda fijo en alumno…»), pero los chips de los otros tres tipos conservan el **mismo aspecto que uno habilitado** — color pleno y cursor de botón — y al hacer clic simplemente no pasa nada. **Detectado el 17-ago por el ejecutor humano de F-32, que pensó que el sistema fallaba** antes de leer la explicación: la señal visual contradice la regla, y quien cobra con prisa no lee. El arreglo es marcar `disabled` los chips no elegibles cuando `usuario_es_menor` (un atributo en `VistaAdmin.tsx`, FormPago), con lo que heredan el estilo atenuado que ya existe. NO se corrigió en el momento a propósito: habría cambiado la versión bajo prueba a media sesión. | Menor | **Corregido y verificado en pantalla** (17-ago: solo CSS, .select-chip:disabled con opacidad y cursor not-allowed; el botón gris salió retratado en la captura de la sonda de D-09 el mismo día del deploy) | `1181050` |

## Cierre de la actividad

- [ ] 100 % de los casos P aprobados.
- [ ] 100 % de los casos E aprobados.
- [ ] ≥ 95 % de los casos F aprobados.
- [ ] Casos A ejecutados y reflejados en el checklist legal E6.
- [ ] Casos U ejecutados con personal real.
- [ ] Defectos críticos y mayores corregidos y reejecutados.

| | Nombre | Firma | Fecha |
|---|---|---|---|
| **Ejecutó** | Gerardo Sánchez | | |
| **Revisó** | Miguel Ángel González Pacheco | | |
